{% extends 'base.html.twig' %}

{% block body %}
    <h1>Manga creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <input type="submit" value="New" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('manga_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
